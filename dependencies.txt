

windows, cpu: conda install pytorch torchvision torchaudio cpuonly -c pytorch
windows, gpu: conda install pytorch torchvision torchaudio cudatoolkit=11.3 -c pytorch
linux, gpu: conda install pytorch torchvision torchaudio cudatoolkit=11.3 -c pytorch


conda install -c conda-forge sentence-transformers 


linux: use pip for transformers and sentence-transformers

To install scikit-hubness run:
python -m pip install git+https://github.com/VarIr/scikit-hubness.git

or download git repository from https://github.com/VarIr/scikit-hubness/tree/main 
to a folder, e.g. 'install_dir'
and install using 
pip install -e /install_dir/scikit-hubness




conda install -c conda-forge statsmodels


conda install -c conda-forge umap-learn

conda install -c conda-forge nmslib

